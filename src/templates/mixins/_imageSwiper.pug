mixin imageSwiper(classname, ...slides)
    .swiper.mt-40(class=classname)
        .swiper-wrapper
            +sliderList('image-slider','image-slider', ...slides)
        .swiper-pagination
        .swiper-button-next
        .swiper-button-prev